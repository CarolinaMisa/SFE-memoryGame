import React from 'react';
import { ReactComponent as Restart} from "../../img/retry.svg";

function Retry() {
    return (
        <div className="Retry">
            <Restart />
        </div>
    );
}

export default Retry;